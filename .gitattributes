# Normalise all text files to LF in the repo (works on Windows + Linux/TrueNAS)
* text=auto eol=lf

# Shell scripts must always be LF (bash on TrueNAS will break with CRLF)
*.sh text eol=lf

# Explicitly binary
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.woff binary
